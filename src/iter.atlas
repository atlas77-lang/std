//! **Outdated**. Placeholder for a generic array iterator. Not imported by `lib.atlas`, don't use it.
//! See `std::iterator` for the concept it is meant to implement.


import "vector.atlas";
import "optional.atlas";
import "string.atlas";
import "memory.atlas";

namespace std {
    //! An iterator over an array of `T`.
    public struct iter<T> {
        
    }

}